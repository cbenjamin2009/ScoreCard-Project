# Default spreadsheet used during local development; wrap the path if it contains spaces.
SOURCE_SPREADSHEET="assets/IT Scorecard.xlsx"

# Controls logging verbosity and toggles any dev-only helpers.
NODE_ENV=development

# Cache behavior for parsed scorecard data (set to "off" to disable caching).
SCORECARD_CACHE_MODE=file
SCORECARD_CACHE_DIR=.cache

# Directory where build artifacts are written; keep in sync with npm run build.
OUTPUT_DIR=dist
